export default function RatingPanel({ professor }) {
  return (
    <section>
      <h1>Calificar a {professor.name}</h1>
      <p>El formulario de calificación se agregará acá.</p>
    </section>
  );
}
